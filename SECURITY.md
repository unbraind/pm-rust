# Security policy

`pm-rust` is pre-release and must be treated as read-only until its mutation,
locking, recovery, and merge conformance gates are complete.

Please report vulnerabilities privately through GitHub security advisories.
Do not place credentials, private workspace data, host paths, or live service
payloads in public issues, fixtures, commits, or pull requests.
